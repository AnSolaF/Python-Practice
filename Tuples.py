#Tupla
#Conjunto ordenado e inmutable de elementos del mismo o diferente tipo.
#En estas no se puede añadir ni quitar cosas, no se puede modificar

my_tuple = (53, 'Andres', 7.5, True)
print(type(my_tuple))

print(my_tuple[1])
print(my_tuple.index())
